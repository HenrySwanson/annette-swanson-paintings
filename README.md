# annette-swanson-paintings
Mom's website

Built with Zola; use `zola build` to rebuild it and `zola serve` to serve it.

Original photos go in `paintings_raw`, and trimmed/unskewed photos go in `paintings`.
